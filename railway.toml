[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Runs migrations before each new deploy goes live.
# If this fails, Railway aborts the deploy and keeps the old version running.
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
